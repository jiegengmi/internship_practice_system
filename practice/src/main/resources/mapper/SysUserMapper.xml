<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ikikyou.practice.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.ikikyou.practice.entity.system.SysUser">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="name" column="name" jdbcType="VARCHAR"/>
            <result property="nickName" column="nickName" jdbcType="VARCHAR"/>
            <result property="age" column="age" jdbcType="SMALLINT"/>
            <result property="sex" column="sex" jdbcType="SMALLINT"/>
            <result property="tel" column="tel" jdbcType="VARCHAR"/>
            <result property="email" column="email" jdbcType="VARCHAR"/>
            <result property="password" column="password" jdbcType="VARCHAR"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
            <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
            <result property="status" column="status" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,name,nick_name,age,
        sex,tel,email,
        password,create_time,update_time,
        status
    </sql>
    <select id="getUsersByRoleId" resultType="com.ikikyou.practice.entity.system.SysUser">
        select
            id,name,nick_name,age,sex,tel,email,password,create_time,update_time,status
        from
            sys_user u left join user_role_link url on u.id = url.user_id
        where
            role_id=#{roleId}
    </select>
</mapper>
